import PagePlaceholder from "@/components/PagePlaceholder";

export default function Cookies() {
  return (
    <PagePlaceholder
      eyebrow="Legal"
      title="Cookie Policy"
      description="What cookies this site uses — tracking, analytics, login sessions."
      todo={["List actual cookies once analytics/auth tools are chosen"]}
    />
  );
}
