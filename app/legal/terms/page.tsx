import PagePlaceholder from "@/components/PagePlaceholder";

export default function Terms() {
  return (
    <PagePlaceholder
      eyebrow="Legal"
      title="Terms of Service"
      description="Rules for using the platform, listing a business, and membership."
      todo={["Have this reviewed properly once real data collection begins — not just a generic template"]}
    />
  );
}
